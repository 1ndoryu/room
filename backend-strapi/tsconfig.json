{
  "compilerOptions": {
    "module": "CommonJS",
    "moduleResolution": "node", // Más común y explícito que "Node"
    "lib": ["ES2020"],  // Puedes mantener ES2020 si necesitas características específicas
    "target": "ES2019", // O ES2020, según tu entorno de ejecución
    "strict": true,   // ¡Activa strict mode! Mejora la calidad del código
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "incremental": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "noEmitOnError": true,
    "noImplicitThis": true, // Parte de strict mode, pero es bueno tenerlo explícito
    "outDir": "dist",
    "rootDir": ".",
    "baseUrl": ".",  // Importante: Establece la base para las rutas
    "paths": {
      "@helpers/*": ["helpers/*"], // Define el alias para la carpeta helpers
      "@config/*": ["config/*"] //BUENA PRACTICA, alias para config
    }
  },
  "include": [
    "./",        // Incluye archivos en la raíz
    "./**/*.ts",  // Incluye todos los archivos .ts
    "./**/*.js", // Incluye todos los archivos .js
    "src/**/*.json" // Incluye archivos JSON (buena práctica)
  ],
  "exclude": [
    "node_modules/",
    "build/", // Generalmente no necesitas excluir build
    "dist/",
    ".cache/",
    ".tmp/",
    "src/admin/", // Excluye el código del panel de administración (buena práctica)
    "**/*.test.*",  // Excluye archivos de prueba
    "src/plugins/**" // Excluye plugins (buena práctica si los tienes)
  ]
}